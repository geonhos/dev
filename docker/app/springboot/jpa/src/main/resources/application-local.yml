spring:
  datasource:
    url: jdbc:mariadb://localhost:3308/my
    driver-class-name: org.mariadb.jdbc.Driver
    username: user
    password: password

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080